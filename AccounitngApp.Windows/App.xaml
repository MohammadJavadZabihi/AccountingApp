<Application x:Class="AccounitngApp.Windows.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:AccounitngApp.Windows"
             StartupUri="/Views/Login.xaml">
    <Application.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="Resourcees/Colors.xaml"/>
            </ResourceDictionary.MergedDictionaries>

            <Style x:Key="btnExite" TargetType="Button">
                <Setter Property="Background" Value="Transparent"/>
                <Setter Property="FontFamily" Value="Vazir"/>
                <Setter Property="FontWeight" Value="Light"/>
                <Setter Property="FontSize" Value="17"/>
                <Setter Property="Foreground" Value="#111111"/>
                <Setter Property="Width" Value="30"/>
                <Setter Property="Height" Value="30"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="Button">
                            <Border Background="{TemplateBinding Background}" CornerRadius="8">
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
                <Style.Triggers>
                    <Trigger Property="IsMouseOver" Value="True">
                        <Setter Property="Background" Value="#df3e3e"/>
                    </Trigger>
                </Style.Triggers>
            </Style>

            <Style x:Key="btnLogin" TargetType="Button">
                <Setter Property="FontFamily" Value="Vazir"/>
                <Setter Property="FontWeight" Value="Bold"/>
                <Setter Property="FontSize" Value="17"/>
                <Setter Property="Width" Value="200"/>
                <Setter Property="Height" Value="40"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="Button">
                            <Border Background="{TemplateBinding Background}" CornerRadius="8">
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
                <Style.Triggers>
                    <Trigger Property="IsMouseOver" Value="True">
                        <Trigger.EnterActions>
                            <BeginStoryboard>
                                <Storyboard>
                                    <DoubleAnimation Storyboard.TargetProperty="Width" To="210" Duration="0:0:0.3"/>
                                    <DoubleAnimation Storyboard.TargetProperty="Height" To="50" Duration="0:0:0.3"/>
                                </Storyboard>
                            </BeginStoryboard>
                        </Trigger.EnterActions>

                        <Trigger.ExitActions>
                            <BeginStoryboard>
                                <Storyboard>
                                    <DoubleAnimation Storyboard.TargetProperty="Width" To="200" Duration="0:0:0.3"/>
                                    <DoubleAnimation Storyboard.TargetProperty="Height" To="40" Duration="0:0:0.3"/>
                                </Storyboard>
                            </BeginStoryboard>
                        </Trigger.ExitActions>
                    </Trigger>
                </Style.Triggers>
            </Style>

            <Style x:Key="headerButton" TargetType="Button">
                <Setter Property="Background" Value="Transparent"/>
                <Setter Property="FontFamily" Value="Vazir"/>
                <Setter Property="FontWeight" Value="Light"/>
                <Setter Property="FontSize" Value="25"/>
                <Setter Property="Foreground" Value="#111111"/>
                <Setter Property="Width" Value="30"/>
                <Setter Property="Height" Value="30"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="Button">
                            <Border Background="{TemplateBinding Background}" CornerRadius="8">
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
                <Style.Triggers>
                    <Trigger Property="IsMouseOver" Value="True">
                        <Setter Property="Background" Value="#cfcfcf"/>
                    </Trigger>
                </Style.Triggers>
            </Style>

            <Style x:Key="textStyle" TargetType="TextBlock">
                <Setter Property="FontFamily" Value="Vazir"/>
                <Setter Property="FontWeight" Value="Bold"/>
                <Setter Property="FlowDirection" Value="RightToLeft"/>
            </Style>

            <Style x:Name="TextBoxStyle" TargetType="TextBox">
                <Setter Property="FontFamily" Value="Vazir"/>
                <Setter Property="Foreground" Value="#1e1e1e"/>
                <Setter Property="FontSize" Value="16"/>
                <Setter Property="Padding" Value="5"/>
                <Setter Property="VerticalContentAlignment" Value="Center"/>
                <Setter Property="FontWeight" Value="Light"/>
                <Setter Property="Background" Value="Transparent"/>
            </Style>

            <Style x:Name="PasswordBoxStyle" TargetType="PasswordBox">
                <Setter Property="FontFamily" Value="Vazir"/>
                <Setter Property="Foreground" Value="#1e1e1e"/>
                <Setter Property="Padding" Value="20"/>
                <Setter Property="VerticalContentAlignment" Value="Center"/>
                <Setter Property="FontWeight" Value="Medium"/>
                <Setter Property="Padding" Value="10 0 0 0"/>
                <Setter Property="Background" Value="Transparent"/>
            </Style>
            
            <ControlTemplate x:Key="PasswordboxTempalte" TargetType="{x:Type PasswordBox}">
                <Border Background="{TemplateBinding Background}" x:Name="Bd" BorderBrush="#1e1e1e" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="10">
                    <ScrollViewer Name="PART_ContentHost" SnapsToDevicePixels="{TemplateBinding UIElement.SnapsToDevicePixels}" />
                </Border>
            </ControlTemplate>

            <ControlTemplate x:Key="TextBoxTemplate" TargetType="{x:Type TextBox}">
                <Border Background="{TemplateBinding Background}" x:Name="Bd" BorderBrush="#3a3a3a" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="10">
                    <ScrollViewer Name="PART_ContentHost" SnapsToDevicePixels="{TemplateBinding UIElement.SnapsToDevicePixels}" />
                </Border>
            </ControlTemplate>

        </ResourceDictionary>
    </Application.Resources>
</Application>

    